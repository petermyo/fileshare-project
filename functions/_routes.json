{
  "version": 1,
  "description": "Routes explicitly /api/* paths to Pages Function. All other paths (including / and any unexpected /f/*) are served as static assets by default.",
  "include": [
    "/api/*",
    "/d/*"
  ]
}
