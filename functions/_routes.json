{
  "version": 1,
  "description": "Only define function routes; all other paths (including /) fall through to static assets by default.",
  "routes": [
    { "pattern": "/api/*", "destination": "function" },
    { "pattern": "/f/*", "destination": "function" }
  ]
}
