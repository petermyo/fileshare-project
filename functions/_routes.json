{
  "version": 1,
  "description": "Explicitly routes /api and /f paths to Pages Function; all other paths (including /) fall through to static assets.",
  "include": [
    "/*"
  ],
  "exclude": [
    "/api/*",
    "/f/*"
  ]
}
